# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-weather-service
metadata:
  template: mcp-weather-service@0.0.1-beta

services:
  mcp-weather:
    project: .
    language: docker
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .

infra:
  provider: bicep
  path: ./infra
