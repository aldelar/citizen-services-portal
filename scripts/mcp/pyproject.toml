[project]
name = "mcp-scripts"
version = "0.1.0"
description = "MCP server test and utility scripts"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.25.0",
]

[dependency-groups]
dev = []
